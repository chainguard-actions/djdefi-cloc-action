name: Test basic cloc usage
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Run cloc with no options
        id: cloc
        uses: .
      - name: Assert cloc ran successfully
        run: |
          echo "cloc step completed successfully"
          echo "PASS"
