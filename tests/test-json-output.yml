name: Test cloc with JSON output option
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Run cloc with --json option
        id: cloc
        uses: .
        with:
          options: '--json'
      - name: Assert cloc ran successfully with JSON output
        run: |
          echo "cloc with --json option completed successfully"
          echo "PASS"
