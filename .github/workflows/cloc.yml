name: Count Lines of Code

# Controls when the action will run. Triggers the workflow on push or pull request 
# events but only for the main branch
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

# Minimal permissions — only read access to repository contents is required
permissions:
  contents: read

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  cloc:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
    - uses: actions/checkout@f43a0e5ff2bd294095638e18286ca9a3d1956744 # v3

    # Runs djdefi/cloc-action@main
    - name: Count Lines of Code (cloc)
      uses: djdefi/cloc-action@4f31c3e25371ff21880fa77aeb0925ce6fc0891b # main
    
    #- name: Upload cloc output as a build artifact
    #  uses: actions/upload-artifact@v2
    #  with:
    #    name: cloc
    #    path: cloc.txt
